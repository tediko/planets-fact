import { Nav, List } from './NavDesktopStyles';
import Items from './ItemControler';

const NavDesktop = () => {
    return (
        <Nav>
            <List>
                <Items />
            </List>
        </Nav>
    );
};

export default NavDesktop;
